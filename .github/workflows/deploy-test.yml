name: Deploy to Test

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: test
      radix-environment: test
    secrets: inherit
